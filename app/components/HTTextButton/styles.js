import {StyleSheet} from 'react-native'

import {fonts} from '../../constants'

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    justifyContent: 'center',
  },
  text: {fontSize: 16, fontFamily: fonts.varelaRound},
})
